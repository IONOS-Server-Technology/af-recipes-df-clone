.omc/
.venv/
# CI installs the af-validate CLI from af-api into this venv; ignored so
# reproducing a validation run locally leaves the tree clean.
.venv-af-api/
__pycache__/
# gen-health-check-conf.py writes the per-combination ImageTester config here at
# run time. It has to live in tests/ so [include_auto_inject] resolves, but it is
# a build artifact — scripts/gen-health-check-conf.py is the source.
tests/recipe-health-check.combo.conf
