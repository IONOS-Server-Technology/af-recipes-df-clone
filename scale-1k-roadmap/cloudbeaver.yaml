slug: cloudbeaver
name: CloudBeaver
source_description: "Web-based database management tool for comprehensive data administration"

upstream:
  url: https://dbeaver.com/products/cloudbeaver/
  repo_url: https://github.com/dbeaver/cloudbeaver
  license: Apache-2.0
  docker_image: dbeaver/cloudbeaver
  compose_file_url: null
  compose_file_notes: >
    No official docker-compose file exists for CloudBeaver Community Edition
    (the image recorded above, dbeaver/cloudbeaver). The project's own
    deployment wiki page for CE
    (github.com/dbeaver/cloudbeaver/wiki/CloudBeaver-Community-deployment-from-docker-image)
    documents only a bare `docker run` command with a workspace volume
    mount; the Docker Hub page for dbeaver/cloudbeaver links to that same
    wiki page and likewise has no compose file. DBeaver does publish an
    official docker-compose setup, but only for CloudBeaver Enterprise
    Edition, in the separate github.com/dbeaver/cloudbeaver-deploy repo —
    that is a different product/image (EE, not the CE image this candidate
    is scoped to), so it is not adopted here.

version_health:
  latest_version: "26.1.4"
  latest_release_date: "2026-08-03"
  release_cadence: active
  eol_status: active
  notes: >
    Latest patch (26.1.4) shipped two days before this analysis (2026-08-05),
    with 26.1.0 -> 26.1.4 all landing within the last ~2 months and an "ea"
    (early access) tag updated within the last day. 8k+ commits on the devel
    branch, 5.1k stars, actively triaged issues/PRs. This is a well-maintained,
    fast-moving project, not a candidate for dormant/EOL flags.

catalogue_fit:
  categories: [database, developer-tools]
  has_web_ui: true
  reverse_proxy_compatible: true
  overlaps_with: [adminer]
  differentiation: >
    Same niche as the existing apps-gaps/adminer.yaml candidate: a generic
    web UI for browsing/administering databases the customer runs elsewhere
    (their own MySQL/Postgres, or another recipe's internal DB). CloudBeaver
    is heavier (Java server, ~400MB image, persistent workspace) and more
    actively maintained than Adminer, with a richer UI (multi-connection
    management, SQL editor, ER diagrams) versus Adminer's single-page,
    per-visit connection form. The two are direct substitutes for the same
    customer need rather than complementary.
  usefulness: medium
  usefulness_rationale: >
    Same audience as Adminer — developers/power users who self-host a
    database and want a management UI without installing DBeaver locally —
    so it doesn't expand the catalogue's addressable use case, just offers a
    heavier alternative. Worth having only if the product wants two DB-admin
    options at different weight/feature tiers, not as a first pick.

installability:
  recipe_type_guess: docker-compose
  secrets_required_at_first_boot:
    - CB_ADMIN_NAME
    - CB_ADMIN_PASSWORD
    - basic_auth_password
  secrets_expressible_today:
    - secret: CB_ADMIN_NAME
      mechanism: server_generated_no_customer_input
    - secret: CB_ADMIN_PASSWORD
      mechanism: server_generated_no_customer_input
    - secret: basic_auth_password
      mechanism: traefik_basic_auth
  blocking_issues: []
  installable_today: true
  recommendation: build_now

summary: >
  CloudBeaver is a single-container, single-HTTP-port (8978) Java web app
  with a persistent workspace volume, Apache-2.0 licensed and under active,
  fast-paced development (26.1.4 shipped 2 days before this analysis). It
  duplicates the same generic-DB-admin-UI niche already covered by the
  apps-gaps/adminer.yaml candidate, just with a heavier footprint and richer
  feature set. Unlike Adminer, CloudBeaver has its own persistent admin
  account, but it is not un-provisionable: CloudBeaver officially supports
  presetting the admin username/password via
  {WORKSPACE}/conf/initial-data.conf (adminName/adminPassword keys),
  applied at first server initialization in place of the interactive setup
  wizard (https://dbeaver.com/docs/cloudbeaver/Initial-data-configuration/).
  That file only accepts plaintext, not a hash, so generated_from can't
  project the customer's root password into it directly, but a
  server-generated random value written to initial-data.conf at container
  start works the same way CKAN's CKAN_SYSADMIN_NAME/PASSWORD do, with the
  whole app additionally gated behind Traefik basic_auth
  (root-password-derived) so the admin account is never customer-facing
  either way. Installable today with no blocking issues, so this is a
  build_now; it overlaps with Adminer on niche, but overlap is not a
  recommendation input.
