slug: lago
name: Lago
source_description: Open-source metering and usage-based billing platform

upstream:
  url: https://getlago.com
  repo_url: https://github.com/getlago/lago
  license: AGPL-3.0
  docker_image: getlago/api, getlago/front (plus getlago/postgres-partman, getlago/lago-gotenberg)

version_health:
  latest_version: v1.51.0
  latest_release_date: "2026-07-27"
  release_cadence: active
  eol_status: active
  notes: >
    9 days since latest release as of 2026-08-05; repo had 5 releases in the
    3 months prior (v1.47.0 through v1.51.0), roughly one every 2-3 weeks.
    Not archived, 10.3k stars, main branch pushed 2026-08-03. Clearly active,
    well-maintained upstream.

catalogue_fit:
  categories: [finance, billing, developer-tools]
  has_web_ui: true
  reverse_proxy_compatible: true
  overlaps_with: []
  differentiation: >
    No recipe in the current 24-app catalogue covers billing/metering/invoicing
    for usage-based SaaS products. This is a genuinely new category — appeals
    to customers building their own SaaS/API product who want self-hosted
    Stripe-Billing-style infrastructure rather than a general productivity or
    infra tool.
  usefulness: medium
  usefulness_rationale: >
    High differentiation (fills a real gap) but a narrower audience than most
    of the catalogue — useful mainly to customers running their own metered
    SaaS/API business, not general self-hosters.

installability:
  recipe_type_guess: docker-compose
  secrets_required_at_first_boot: []
  secrets_expressible_today: []
  blocking_issues: []
  installable_today: true
  recommendation: build_now

summary: >
  Lago is an active, well-maintained (v1.51.0, released 2026-07-27, ~biweekly
  cadence, 10.3k stars, AGPL-3.0) open-source usage-based billing platform
  with no equivalent in the current catalogue. Its stack is a 7-service
  docker-compose (Postgres, Redis, api, front, api-worker, api-clock, pdf/
  gotenberg) but every service is standard container orchestration — the only
  "secret" at boot is a self-signed RSA private key and DB/Redis credentials,
  all of which the platform can generate server-side with no customer input
  needed, same as n8n's encryption key. There is no fixed admin credential to
  project: like n8n's owner-setup wizard, Lago's first visitor creates the
  organization via an open in-app signup form post-deploy, so no
  generated_from or basic_auth gate is structurally required to make it
  bootable (though, as with n8n, the open signup page is technically racy
  until the customer visits it — not treated as a blocker here since it
  matches accepted precedent). The API (port 3000) and front (port 80) are
  both plain HTTP, so both can sit behind Traefik on two subdomains, matching
  the multi-public-HTTP-port pattern already used by gitea/runtipi/wud.
  Recommend build_now.
