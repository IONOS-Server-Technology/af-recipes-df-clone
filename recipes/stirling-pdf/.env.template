# Stirling PDF uses no customer-specific variables. SECURITY_ENABLELOGIN and
# DOCKER_ENABLE_SECURITY are static deployment choices set directly in
# docker-compose.yaml. Basic-auth credentials are managed by Traefik, not stored here.
# The app hostname is supplied automatically as AF_APP_DOMAIN.
