# Internal database secret — generated for this server during installation.
# Empty in the delivered file so no two servers share the same credential.
POSTGRES_PASSWORD=
# The platform derives this from the server root password at first boot via argon2.
# Do not edit — it is written by the platform before install.sh runs.
DJANGO_ADMIN_PASSWORD=
