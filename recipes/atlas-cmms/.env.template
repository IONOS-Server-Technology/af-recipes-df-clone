# Database password — generated for this server during installation.
# Empty in the delivered file so no two servers share the same credential.
POSTGRES_PASSWORD=
# JWT signing key — generated for this server during installation.
# Empty in the delivered file so no two servers share the same key.
JWT_SECRET_KEY=
# MinIO root password — generated for this server during installation.
# Empty in the delivered file so no two servers share the same credential.
MINIO_PASSWORD=
