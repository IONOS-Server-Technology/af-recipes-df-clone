# GoWA has no secrets configurable via environment variables.
# The Traefik basic_auth gate (managed by the platform) provides the auth
# boundary. GoWA's own APP_BASIC_AUTH is a plaintext user:pass compare that
# is not compatible with generated_from — it is intentionally left unset here.
# AF_APP_DOMAIN is injected by the platform at render time.
