# PostgreSQL password — generated per-server during install.sh.
# Empty here so every server receives its own unique value.
POSTGRES_PASSWORD=

# Master secret key used to derive HTTP session and subsystem secrets.
# Generated per-server during install.sh.
PENPOT_SECRET_KEY=
