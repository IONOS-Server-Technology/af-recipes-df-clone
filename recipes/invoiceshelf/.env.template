# InvoiceShelf has no app-level credentials injected at install time.
# The admin account is created through the browser-based first-run setup wizard
# the first time the app is accessed. The Traefik basic_auth layer (configured
# separately by the platform) closes the setup wizard to unauthenticated visitors.
