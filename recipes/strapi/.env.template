# Strapi application secrets — generated by install.sh on first boot.
# Do not set these manually; install.sh fills them with per-server random values.
APP_KEYS=
API_TOKEN_SALT=
ADMIN_JWT_SECRET=
TRANSFER_TOKEN_SALT=
JWT_SECRET=
