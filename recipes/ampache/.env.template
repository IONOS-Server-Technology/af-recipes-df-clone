# Generated during installation — empty here so every server gets its own credentials.
MYSQL_ROOT_PASSWORD=
DB_PASSWORD=

# Filled in at compose time from your server password (bcrypt hash).
# Log in to Ampache with the server password you already have.
AMPACHE_ADMIN_PASSWORD=
