# Secrets are generated for this server during installation and written back here.
# They are empty in the delivered file, so no two servers share a secret.
LIBREDESK_SYSTEM_USER_PASSWORD=
POSTGRES_PASSWORD=
ENCRYPTION_KEY=
