# Intentionally empty. This application's Compose file references no variables from
# this template — GRIST_IN_SERVICE and GRIST_DEFAULT_EMAIL are static config hardcoded
# in docker-compose.yaml, and the only platform-injected variable (AF_APP_DOMAIN) is
# not needed by Grist itself (Traefik handles public routing).
