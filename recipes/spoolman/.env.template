# Spoolman has no authentication of its own and declares no parameters.
# The platform writes AF_APP_DOMAIN and other reserved keys into .env at
# render time. There is nothing else to configure here.
