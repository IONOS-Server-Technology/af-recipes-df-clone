# Secrets generated by install.sh — empty here so no two servers share credentials.
SECRET_JWT_SECRET=
SECRET_ROOT_PASSWORD=
# Static root username — not a secret, no generation required.
SECRET_ROOT_USER=root
