# Secrets generated by install.sh at first boot — empty here so no two servers share a value.
JWT_SECRET=
INTERNAL_API_KEY=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
REDIS_PASSWORD=
COUCHDB_PASSWORD=
# Static username for the embedded CouchDB; no customer input required.
COUCHDB_USER=budibase
