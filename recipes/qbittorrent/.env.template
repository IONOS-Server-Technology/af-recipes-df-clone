# qBittorrent — no user-configurable parameters in this recipe.
# PUID/PGID are fixed in docker-compose.yaml (1000/1000).
# The WebUI admin password is auto-generated at first startup; retrieve it
# from the container logs, then change it via Tools → Options → Web UI.
# A Traefik basic_auth gate covers the WebUI until custom credentials are set.
