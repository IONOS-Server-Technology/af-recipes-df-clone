# Intentionally empty. Mazanoke is a fully client-side image optimizer — the server
# serves static assets only and requires no configuration variables. All image
# processing runs in the browser using WebAssembly; no files are stored server-side.
# Access control is provided by Traefik basic auth (see metadata.yaml).
# The app's hostname is supplied automatically as AF_APP_DOMAIN.
