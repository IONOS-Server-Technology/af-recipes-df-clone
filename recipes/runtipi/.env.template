# Generated per VM by install.sh on first boot. Empty because nothing substitutes values
# into this file (IF-944) — a literal JWT_SECRET here would let anyone with repo access
# mint valid tokens for every customer VM.
JWT_SECRET=
POSTGRES_PASSWORD=
