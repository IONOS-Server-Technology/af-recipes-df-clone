id: open-webui
enabled: true
display_name: Open WebUI
description: Browser-based chat interface for local AI models, bundled with Ollama for model management and inference.
short_description:
  en: "Chat with local AI models in your browser — no cloud, no data leaves your server. Bundled with Ollama for model management."
  de: "KI-Modelle im Browser nutzen — lokal auf Ihrem Server, ohne Cloud. Inklusive Ollama zur Modellverwaltung."
  es: "Chatea con modelos de IA locales desde tu navegador — sin nube, los datos no salen de tu servidor. Incluye Ollama para gestión."
  fr: "Discutez avec des modèles d'IA locaux depuis votre navigateur — sans cloud, les données restent sur votre serveur. Ollama inclus."
  it: "Chatta con modelli IA locali dal browser — niente cloud, i dati restano sul tuo server. Ollama incluso per la gestione modelli."
  nl: "Chat met lokale AI-modellen in je browser — geen cloud, data blijft op je server. Inclusief Ollama voor modelbeheer."
  pl: "Rozmawiaj z lokalnymi modelami AI w przeglądarce — bez chmury, dane zostają na serwerze. W zestawie Ollama do zarządzania modelami."
categories:
  - ai
app_version: "0.11.0"
recipe_version: "1.1.0"
recipe_type: docker-compose
upstream_url: https://github.com/open-webui/open-webui

app_min_ram_mb: 8192
app_min_disk_mb: 51200


# basic_auth (IF-1312): closes a gap flagged in Björn Jäger's review of this story
# (Finding 2) — the 2026-08-04 PM decision names open-webui alongside n8n, wud and
# openclaw, but this recipe never got the flag.
#
# What this protects: a fresh Open WebUI has onboarding pending and signup enabled, so
# an unclaimed instance (fronting an attached Ollama container) can be taken over by
# anyone who reaches the URL before the customer's first visit — the same class of
# exposure the n8n and wud comments above document.
#
# What it costs: the whole router goes behind the prompt, including its OpenAI-compatible
# API, so any script or client calling the API directly gets a 401 too, same as n8n's
# webhooks. The customer turns the prompt off once their account exists, with
# '/root/auth.sh off open-webui', named in the password prompt.
ports:
  - port: 8080
    protocol: tcp
    public: true
    basic_auth: true
    description: Open WebUI Web Interface (routed via Traefik on 80/443)

parameters:
  - name: WEBUI_SECRET_KEY
    display_name: Secret Key
    type: password
    description: Secret key for JWT token signing.


# Logo (rendered in app catalogue thumbnails)
logo_url: https://appfactory.s3.eu-central-3.ionoscloud.com/recipes/open-webui/1.1.0/logo.svg
logo_sha256: 164811064d375a9f9d2b0512d1c0d438b3355bafe009a7635fa5a7d1adf0cbc5
logo_license: trademark-nominative-fair-use
logo_source: https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons@main/svg/open-webui.svg
