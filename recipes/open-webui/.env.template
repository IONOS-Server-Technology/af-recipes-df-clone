# Generated per VM by install.sh on first boot and written back here. It ships empty on
# purpose: nothing substitutes values into this file (IF-944), so a literal committed here
# would sign every customer's sessions with a key anyone can read out of the repo.
WEBUI_SECRET_KEY=
