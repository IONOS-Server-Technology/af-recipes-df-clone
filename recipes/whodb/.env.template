# Encryption key for WhoDB's saved database connection profiles. Generated
# automatically by install.sh on first install — a fixed value here would be
# identical across every server in the fleet, which defeats the purpose.
WHODB_ENCRYPTION_KEY=
