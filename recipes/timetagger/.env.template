# Filled in automatically with a bcrypt hash of your server password; install.sh
# prepends the fixed "admin" username to form "admin:<hash>" before first start.
# Log in at the app URL with username "admin" and your server password.
TIMETAGGER_CREDENTIALS=
