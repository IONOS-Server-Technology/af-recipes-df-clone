# Livebook's built-in token authentication is disabled at compose time
# (LIVEBOOK_TOKEN_ENABLED=false) so that the Traefik basic-auth gate
# declared in metadata.yaml (basic_auth: true) is the sole access control.
#
# No customer-supplied secrets are required. The platform injects
# AF_APP_DOMAIN at render time for Traefik routing.
