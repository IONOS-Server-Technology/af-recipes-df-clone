# Kanboard uses no configurable parameters.
# The platform writes AF_APP_DOMAIN into .env at deploy time; this recipe
# does not reference it because Kanboard does not need its public URL at startup.
