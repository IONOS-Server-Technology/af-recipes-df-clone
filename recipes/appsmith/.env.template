# Intentionally empty. This application's Compose file references no variables from
# this template — the only substitution is AF_APP_DOMAIN, which the platform writes
# into .env at render time. Appsmith's internal Mongo, Redis, and Postgres passwords
# are server-generated by the container itself and require no customer input.
