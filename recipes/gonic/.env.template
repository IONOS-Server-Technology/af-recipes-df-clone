# All Gonic paths are set statically in docker-compose.yaml and mounted under
# /opt/gonic/. The platform provides AF_APP_DOMAIN automatically. Gonic's
# fixed admin/admin default is secured at the network edge by the Traefik
# basic-auth gate declared in metadata.yaml (basic_auth: true) — nothing more
# to configure here before first boot.
