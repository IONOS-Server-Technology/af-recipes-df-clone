# This application's Compose file references no variables, so there is nothing
# to configure here. Yucca creates its admin account via a first-visit web
# setup wizard and requires no secrets at install time.
