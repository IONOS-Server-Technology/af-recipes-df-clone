# Session signing secret — generated for this server during installation.
# Empty in the delivered file so no two servers share the same secret.
SESSION_SECRET=
# Database encryption key — generated for this server during installation.
# Empty in the delivered file so no two servers share the same secret.
DB_ENCRYPTION_KEY=
