# Intentionally empty. Silex in filesystem-connector mode requires no
# customer-supplied secrets or configuration values — storage and hosting paths
# are fixed in docker-compose.yaml, and the app's domain is supplied automatically
# as AF_APP_DOMAIN by the platform at render time.
