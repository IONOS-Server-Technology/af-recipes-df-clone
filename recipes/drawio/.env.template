# Intentionally empty. draw.io is a fully client-side editor — the server serves
# static assets only and requires no configuration variables. Diagrams are saved
# locally in the browser or to optional third-party integrations (Google Drive,
# OneDrive, GitHub) and no server-side state is kept. The app's hostname is
# supplied automatically as AF_APP_DOMAIN.
