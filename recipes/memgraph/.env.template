# No per-VM secrets or customer parameters are required for Memgraph.
# Memgraph Lab (port 3000) is gated by Traefik basic auth using the
# platform's server password — no separate application credentials are
# needed here. AF_APP_DOMAIN is written by the platform at render time.
