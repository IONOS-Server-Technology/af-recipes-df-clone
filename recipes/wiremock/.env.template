# WireMock has no application-level credentials or configuration to set here.
# Access is controlled by the reverse proxy in front of it — the customer's
# server password gates the /__admin API and stub endpoints via Traefik basic auth.
