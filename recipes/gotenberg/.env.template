# Intentionally empty. Gotenberg is a stateless REST API with no server-side
# configuration required. The Traefik basic-auth gate is configured at the
# platform level via the port declaration in metadata.yaml (basic_auth: true),
# not through env vars here. The app's hostname is supplied automatically as
# AF_APP_DOMAIN.
