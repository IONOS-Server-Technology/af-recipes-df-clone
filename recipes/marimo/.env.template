# Marimo's built-in token authentication is disabled via --no-token; the UI is
# protected by Traefik basic auth using the server password you already have.
# There are no secrets to configure here — the platform writes AF_APP_DOMAIN
# into .env automatically.
