# Intentionally empty. Web-Check is fully stateless — it requires no configuration
# variables to boot or use its core features. Optional API keys (Shodan, WhoAPI,
# PageSpeed, etc.) that unlock additional data sources are not required and should
# not be set here without explicit customer intent.
# The app's hostname is supplied automatically as AF_APP_DOMAIN.
