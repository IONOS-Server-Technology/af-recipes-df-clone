# Intentionally empty. This application's Compose file references no variables, so there
# is nothing to configure here. The application's own hostname is supplied automatically
# as AF_APP_DOMAIN. Grocy's default admin/admin credentials are secured at the network
# edge by Traefik basic auth, which is wired automatically from metadata.yaml basic_auth: true.
