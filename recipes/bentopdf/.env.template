# BentoPDF is stateless and requires no secrets of its own.
# All PDF processing happens client-side in the browser via WASM — no file content
# reaches the server. Access is controlled by the Traefik basic-auth gate declared
# in metadata.yaml (basic_auth: true) — the customer's server password is applied
# there, not here.
