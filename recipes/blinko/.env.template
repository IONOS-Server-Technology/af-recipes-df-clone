# Database password — generated for this server during installation.
# Empty in the delivered file so no two servers share the same credential.
POSTGRES_PASSWORD=
# NextAuth.js signing secret — generated for this server during installation.
# Empty in the delivered file so no two servers share the same secret.
NEXTAUTH_SECRET=
