# Radicale uses no configurable parameters.
# Auth is handled by Traefik basic_auth; Radicale's own auth is disabled via
# the RADICALE_CONFIG_AUTH_TYPE environment variable in docker-compose.yaml.
# The platform writes AF_APP_DOMAIN into .env at deploy time; this recipe
# does not reference it because Radicale does not need its public URL at startup.
