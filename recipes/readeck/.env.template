# Intentionally empty. This application's Compose file references no customer-supplied
# variables — the allowed-hosts domain is supplied automatically as AF_APP_DOMAIN, and
# the reverse-proxy flag is a static literal. There is nothing to configure here.
