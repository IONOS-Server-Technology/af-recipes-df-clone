# Intentionally empty. CyberChef is a fully client-side tool — the server serves
# static assets only and requires no configuration variables. All operations run
# in the browser on data the customer pastes in; no server-side state is kept.
# The app's hostname is supplied automatically as AF_APP_DOMAIN.
