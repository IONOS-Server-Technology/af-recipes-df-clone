# Database password — generated for this server during installation.
# Empty in the delivered file so no two servers share the same credential.
DB_PASSWORD=
# MariaDB root password — generated for this server during installation.
# Internal credential; not exposed to the customer.
MYSQL_ROOT_PASSWORD=
