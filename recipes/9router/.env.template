# Dashboard admin password — filled by the platform from your server password.
# Log in to the 9Router dashboard with the same password you use for your server.
INITIAL_PASSWORD=

# JWT session signing secret — generated by install.sh on first boot.
# The customer never needs to see or set this value.
JWT_SECRET=
