# Yopass has no customer-configurable parameters.
# Secrets are encrypted client-side in the browser and stored ephemerally in
# Memcached; the server never sees plaintext and requires no secrets at boot.
