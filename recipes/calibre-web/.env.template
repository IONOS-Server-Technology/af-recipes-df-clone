# Calibre-Web — no user-configurable parameters in this recipe.
# PUID/PGID/TZ are fixed in docker-compose.yaml.
# The app ships with a hardcoded admin/admin123 login. A Traefik basic_auth
# gate protects the UI until you change that credential via the app's Settings
# → Edit Basic Configuration → Admin password, then run:
#
#     /root/auth.sh off calibre-web
