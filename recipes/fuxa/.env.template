# FUXA has no secrets configurable via environment variables.
# The Traefik basic_auth gate (managed by the platform) covers the app's
# fixed default login (admin/123456) until the customer changes it in-app.
# AF_APP_DOMAIN is injected by the platform at render time.
