# MeshCentral needs no additional configuration here. The application hostname is
# supplied automatically as AF_APP_DOMAIN. The first visitor to the web UI creates
# the admin account (first-run flow); afterwards, ALLOW_NEW_ACCOUNTS=false (set in
# docker-compose.yaml) prevents further self-registration.
