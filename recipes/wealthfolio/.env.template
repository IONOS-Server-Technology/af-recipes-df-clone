# WF_AUTH_PASSWORD_HASH is injected by the platform (argon2:ROOT_PASSWORD projection).
# Log in with your server's root password.
WF_AUTH_PASSWORD_HASH=
# WF_SECRET_KEY is generated by install.sh on first boot.
WF_SECRET_KEY=
