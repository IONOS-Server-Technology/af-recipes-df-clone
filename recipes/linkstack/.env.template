# LinkStack requires no customer-supplied configuration at install time.
# The application's public hostname is supplied automatically as AF_APP_DOMAIN.
# Admin credentials are created interactively through the first-run setup wizard.
