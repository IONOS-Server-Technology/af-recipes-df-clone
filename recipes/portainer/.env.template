# Intentionally empty. This Compose file references no variables, so the APP_DOMAIN
# placeholder this used to carry never reached the container — it only landed in the VM's
# .env as literal text (IF-944). The app's own FQDN now arrives as the platform's reserved
# AF_APP_DOMAIN. The file stays, because files-required-compose expects it.
