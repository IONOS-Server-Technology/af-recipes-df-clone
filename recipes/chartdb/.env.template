# OPENAI_API_KEY enables the AI-assisted DDL export feature in ChartDB.
# Leave empty (or remove the line) to use ChartDB without the AI feature — it is fully
# functional without it. DB connections are supplied manually per-session in the browser.
OPENAI_API_KEY=
