# This application's Compose file references no variables beyond the platform-reserved
# AF_APP_DOMAIN variable, which the platform writes into .env automatically.
# There is nothing to configure here.
