# Initial ownCloud administrator password.
# Set automatically by the platform from your server password via argon2id — log in with
# username "admin" and that password. Do not change this value manually.
OCIS_ADMIN_PASSWORD=
