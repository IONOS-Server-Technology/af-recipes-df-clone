# mStream's Compose file references no customer-supplied variables.
# Access is gated by Traefik basic-auth (metadata.yaml: basic_auth: true)
# using the customer's server password — no credentials are needed here.
# mStream runs in public mode by default; account setup happens through the
# web UI after first boot.
