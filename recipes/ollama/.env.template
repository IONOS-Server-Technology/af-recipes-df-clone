# Intentionally empty. Ollama's Compose file references no variables, so there is nothing
# to configure here. The application's own hostname is supplied automatically as
# AF_APP_DOMAIN. To pull a model once the server is up, run
# `docker compose exec ollama ollama pull <model>` from /opt/ollama.
