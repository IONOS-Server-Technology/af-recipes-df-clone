# MQTTX Web is a stateless static application with no built-in authentication
# and no secrets of its own. Its Compose file references no variables.
# The app's hostname is supplied automatically as AF_APP_DOMAIN.
# The web UI is protected by the platform's Traefik basic-auth gate (see metadata.yaml).
