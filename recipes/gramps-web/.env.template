# All static settings are baked into docker-compose.yaml: the Redis URLs, the
# default tree name, and the Celery broker config. The app's public hostname is
# supplied automatically by the platform as AF_APP_DOMAIN. There is nothing to
# configure here before first boot.
