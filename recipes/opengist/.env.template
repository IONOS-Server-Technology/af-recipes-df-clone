# Signs session cookies — generated per VM by install.sh on first boot. Empty because
# nothing substitutes values into this file (IF-944); a literal here would let anyone with
# repo access forge a session on every customer VM.
SECRET_KEY=
