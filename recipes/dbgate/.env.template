# DbGate requires no configuration variables. Authentication is handled by the
# platform-managed Traefik basic auth gate; DbGate's own login is intentionally
# left disabled. Saved connections and settings are persisted to /opt/dbgate/data
# on the host. The app's hostname is supplied automatically as AF_APP_DOMAIN.
