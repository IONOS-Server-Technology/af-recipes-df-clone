# Generated per VM by install.sh on first boot. They ship empty because nothing
# substitutes values into this file (IF-944) — a literal committed here would give every
# customer's Anytype backend the same database and object-store credentials.
MONGO_PASSWORD=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
